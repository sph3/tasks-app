import { AddButton } from './AddButton';

export const Footer = () => {
  return (
    <div className='fixed bottom-0 bg-gradient-to-b from-transparent to-ctp-base backdrop-blur-sm h-28 w-full'>
      <AddButton />
    </div>
  );
};
